spring.datasource.url=jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.videmeamentkjybkyoeq
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.maximum-pool-size=5
server.port=${PORT:8080}

notus.qr.hmacSecret=${QR_HMAC_SECRET:R8kT%fZc1!aP@9Jx0#sM5qE^W2B7L&y}
notus.qr.ttlSeconds=60
notus.auth.jwt-secret=${AUTH_JWT_SECRET:local-dev-secret-change-me}
notus.auth.dev-tokens-enabled=${DEV_AUTH_TOKENS_ENABLED:false}
notus.auth.teacher-registration-token-ttl-seconds=${TEACHER_REGISTRATION_TOKEN_TTL_SECONDS:900}
notus.auth.initial-teacher-code=${INITIAL_TEACHER_CODE:}
notus.seed.sample-schedule-enabled=${SAMPLE_SCHEDULE_SEED_ENABLED:false}
notus.seed.sample-quiz-enabled=${SAMPLE_QUIZ_SEED_ENABLED:false}
notus.seed.dev-data-enabled=${DEV_DATA_SEED_ENABLED:false}
gemini.api.key=${GEMINI_API_KEY}
gemini.model=gemini-2.5-flash
app.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:}
app.frontend-base-url=${APP_FRONTEND_BASE_URL:http://localhost:5173}

spring.mail.host=${SMTP_HOST:}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${SMTP_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${SMTP_WRITE_TIMEOUT:5000}
notus.mail.from=${MAIL_FROM:no-reply@notus.local}
notus.mail.from-name=${MAIL_FROM_NAME:Notus}
brevo.api-key=${BREVO_API_KEY:}

sentry.dsn=${SENTRY_DSN:}
sentry.environment=${SENTRY_ENVIRONMENT:local}
sentry.release=${SENTRY_RELEASE:}
sentry.traces-sample-rate=${SENTRY_TRACES_SAMPLE_RATE:0.1}
sentry.send-default-pii=false
